defaults:
  - data_paths: default
  - map_config: vlmaps
  - params: default
  - _self_

scene_id: 0

obs:
  height:
    min: 0.1 # minimum height (m) for obstacle voxels
    max: 0.8 # maximum height (m) for obstacle voxels

mode: '2d' # "2d" or "3d"
