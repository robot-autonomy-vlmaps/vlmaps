defaults:
  - data_paths: default
  - map_config: vlmaps
  - params: default
  - _self_
nav:
  valid_range: 1
  vis: True
  tasks_per_scene: 20
  waitkey: False
scene_id: 3

# indexing params
decay_rate: 0.01
index_2d: True
init_categories: True
